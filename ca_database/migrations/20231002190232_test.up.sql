CREATE TABLE articles(
    key BYTEA PRIMARY KEY ,
    data BYTEA
);

CREATE TABLE courses(
    key BYTEA PRIMARY KEY ,
    data BYTEA
);

CREATE TABLE images(
    key BYTEA PRIMARY KEY ,
    data BYTEA
);

CREATE TABLE videos(
    key BYTEA PRIMARY KEY ,
    data BYTEA
);

CREATE TABLE audios(
    key BYTEA PRIMARY KEY ,
    data BYTEA
);
